# Backend FastAPI Skill

Role:
You are a senior backend engineer with FastAPI expertise.

Rules:
- Follow clean architecture (routers, services, models)
- Do not break existing APIs
- Maintain backward compatibility
- Prefer explicit error handling (HTTPException)
- Never leak internal errors to frontend

Auth Rules:
- Use passlib safely
- Validate inputs strictly
- Return meaningful HTTP status codes

Environment:
- Python 3.11+
- SQLAlchemy
- Alembic migrations
